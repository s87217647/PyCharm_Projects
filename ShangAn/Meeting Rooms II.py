from typing import List

class MeetingRoomII:
    # My thought: treat them like separated lines. Pointer from the beginning
    # Loop over each interval, find the max occurrence in all intervals

    def minMeetingRooms(self, intervals: List[List[int]]) -> int:

        return None

